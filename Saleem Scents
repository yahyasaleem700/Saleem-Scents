import { useState } from "react";

export default function SaleemScents() {
  const [logo, setLogo] = useState(null);
  const [collectionImage, setCollectionImage] = useState(null);

  const handleLogoUpload = (e) => {
    const file = e.target.files?.[0];
    if (file) setLogo(URL.createObjectURL(file));
  };

  const handleCollectionUpload = (e) => {
    const file = e.target.files?.[0];
    if (file) setCollectionImage(URL.createObjectURL(file));
  };

  return (
    <div className="min-h-screen bg-neutral-950 text-white font-sans">
      <header className="sticky top-0 z-50 border-b border-white/10 bg-neutral-950/90 backdrop-blur">
        <div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4">
          <div className="flex items-center gap-3">
            {logo && (
              <img src={logo} alt="Logo" className="h-10 w-10 rounded-xl object-cover" />
            )}
            <div>
              <h1 className="text-2xl font-bold tracking-wide">SALEEM SCENTS</h1>
              <p className="text-xs uppercase tracking-[0.3em] text-neutral-400">
                Natural Perfumery
              </p>
            </div>
          </div>

          <label className="cursor-pointer rounded-xl border border-white/20 px-4 py-2 text-sm hover:border-white/40">
            Upload Logo
            <input type="file" accept="image/*" className="hidden" onChange={handleLogoUpload} />
          </label>
        </div>
      </header>

      <section className="mx-auto grid max-w-7xl gap-12 px-6 py-24 lg:grid-cols-2 lg:items-center">
        <div>
          <p className="mb-4 text-sm uppercase tracking-[0.4em] text-neutral-400">
            Natural Perfumery Brand
          </p>

          <h2 className="mb-6 text-5xl font-bold leading-tight md:text-6xl">
            One winter fragrance in development.
          </h2>

          <p className="mb-8 max-w-xl text-lg leading-relaxed text-neutral-300">
            Saleem Scents is moving into natural perfumery, focusing on raw materials, balance, and depth. Currently working on a single winter fragrance project.
          </p>

          <div className="flex flex-wrap gap-4">
            <a href="#collection" className="rounded-2xl bg-white px-6 py-3 font-medium text-black">
              View Collection
            </a>
            <a href="#contact" className="rounded-2xl border border-white/20 px-6 py-3">
              Contact
            </a>
          </div>
        </div>

        <div className="rounded-[2rem] border border-white/10 bg-gradient-to-br from-neutral-800 to-neutral-900 p-10 text-center">
          <h3 className="mb-2 text-2xl font-semibold">Winter Project</h3>
          <p className="text-sm text-neutral-400">Natural composition • In development</p>
        </div>
      </section>

      <section id="collection" className="mx-auto max-w-7xl px-6 py-20">
        <div className="mb-10 flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
          <div>
            <h2 className="text-4xl font-bold">Decants Collection</h2>
            <p className="text-sm text-neutral-400">Designer perfumes available for decanting</p>
          </div>

          <label className="cursor-pointer rounded-2xl border border-white/20 px-5 py-3 text-sm w-fit">
            Upload Collection Image
            <input type="file" accept="image/*" className="hidden" onChange={handleCollectionUpload} />
          </label>
        </div>

        <div className="rounded-[2rem] border border-white/10 bg-white/5 p-6">
          {collectionImage ? (
            <img src={collectionImage} alt="Collection" className="w-full rounded-2xl object-cover" />
          ) : (
            <p className="text-neutral-400">No collection image uploaded yet.</p>
          )}

          <div className="mt-6 text-sm text-neutral-300">
            We offer decants from a curated selection of designer fragrances from our collection.
          </div>

          <a href="#contact" className="mt-6 inline-block rounded-2xl bg-white px-6 py-3 font-medium text-black">
            Inquire About Decants
          </a>
        </div>
      </section>

      <section id="contact" className="mx-auto max-w-4xl px-6 py-20">
        <div className="rounded-[2rem] border border-white/10 bg-white/5 p-10 text-center">
          <h2 className="mb-6 text-4xl font-bold">Contact</h2>

          <p className="mb-6 text-neutral-300">
            For inquiries or purchases, reach out below.
          </p>

          <div className="flex flex-col items-center gap-4">
            <a className="rounded-2xl bg-white px-6 py-3 font-medium text-black" href="https://instagram.com/saleemscents">
              @saleemscents
            </a>
            <a className="rounded-2xl border border-white/20 px-6 py-3" href="https://saleemscents.shop">
              saleemscents.shop
            </a>
            <a className="rounded-2xl border border-white/20 px-6 py-3" href="mailto:saleemscents.shop@gmail.com">
              saleemscents.shop@gmail.com
            </a>
          </div>
        </div>
      </section>

      <footer className="border-t border-white/10 py-8 text-center text-sm text-neutral-500">
        © 2026 Saleem Scents
      </footer>
    </div>
  );
}
